# nvidia_boot_optimize Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-05-14

## Active Technologies

- Bash (scripts), Kconfig (kernel), DTS (device tree), UEFI EDK2 (bootloader), Buildroot (build system) + NVIDIA L4T flash tools (flash.sh), cross-compiler (aarch64-linux-gnu-gcc), dtc (001-fast-boot-orin)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for Bash (scripts), Kconfig (kernel), DTS (device tree), UEFI EDK2 (bootloader), Buildroot (build system)

## Code Style

Bash (scripts), Kconfig (kernel), DTS (device tree), UEFI EDK2 (bootloader), Buildroot (build system): Follow standard conventions

## Recent Changes

- 001-fast-boot-orin: Added Bash (scripts), Kconfig (kernel), DTS (device tree), UEFI EDK2 (bootloader), Buildroot (build system) + NVIDIA L4T flash tools (flash.sh), cross-compiler (aarch64-linux-gnu-gcc), dtc

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
